Set(varEnvironmentReady,false); Set(varEnvironment,Blank()); IfError(Set(varEnvironment,MILSTRIPLocalDevAPI.GetHealth()); Set(varEnvironmentReady,varEnvironment.environment=AppEnvironment && varEnvironment.ready); true, Set(varEnvironmentReady,false); false); If(!varEnvironmentReady,Set(varMessage,If(!IsBlank(varEnvironment.environment) && varEnvironment.environment<>AppEnvironment,"Wrong environment connection. Contact the administrator.","Environment unavailable. Contact the administrator."))); If(varEnvironmentReady && varMessage in ["Wrong environment connection. Contact the administrator.","Environment unavailable. Contact the administrator."],Set(varMessage,""))
