- IntakeIcon:
    Control: Image
    Properties:
      X: =24
      Y: =16
      Width: =64
      Height: =64
      Image: ='milstrip-app'
- IntakeTitle:
    Control: Label
    Properties:
      X: =104
      Y: =16
      Width: =Parent.Width-600
      Height: =64
      Text: ="MILSTRIP " & If(AppEnvironment="stage","Stage","Prod") & " | Intake"
      Size: =24
- IntakeMessage:
    Control: Label
    Properties:
      X: =24
      Y: =78
      Width: =Parent.Width-48
      Height: =34
      Text: =Coalesce(varMessage, "")
      Live: =Live.Polite
- txtSource:
    Control: Classic/TextInput
    Properties:
      X: =24
      Y: =152
      Width: =Parent.Width-48
      Height: =244
      Default: =""
      Mode: =TextMode.MultiLine
      HintText: ="Paste the original email or ticket text"
      DisplayMode: =If(Coalesce(varBusy,false) || !Coalesce(varEnvironmentReady,false) || Coalesce(varIntakeUncertain,false)
        || !IsBlank(varActiveIntake),DisplayMode.Disabled,DisplayMode.Edit)
      AccessibleLabel: ="Original email or ticket text"
      TabIndex: =0
      FocusedBorderThickness: =3
      FocusedBorderColor: =RGBA(0,0,0,1)
- btnSubmit:
    Control: Classic/Button
    Properties:
      X: =24
      Y: =490
      Width: =210
      Height: =44
      Text: ="Submit intake"
      OnSelect: '=If(!Coalesce(varBusy,false) && Coalesce(varEnvironmentReady,false),Set(varBusy,true); Set(varFlow,Blank());
        If(IsError(Set(varSourceId,Text(GUID()))),If(IsError(varFlow),Set(varFlow,{ok:false,status:0,error_code:"TRANSPORT_UNKNOWN",error_message:"",request_id:"",result_json:""}));
        Set(varIntakeUncertain,!(Coalesce(varFlow.status,0) in [400,401,403,409,422])); Set(varMessage,If(Coalesce(varFlow.status,0)=409,"Intake
        blocked. Resume your unfinished intake, or ask an administrator to override a duplicate submitted within
        2 hours.","Submission outcome is unknown. Keep the text and compare this source ID in recent requests: "
        & varSourceId)); false,If(IsError(Set(varFlow,MILSTRIPStageBroker.Run("CreateIntakeRequest",JSON({source_type:"PASTE",source_id:varSourceId,source_text:txtSource.Text,duplicate_override_reason:If(chkDuplicateOverride.Value,txtOverrideReason.Text,Blank())},JSONFormat.Compact)))),If(IsError(varFlow),Set(varFlow,{ok:false,status:0,error_code:"TRANSPORT_UNKNOWN",error_message:"",request_id:"",result_json:""}));
        Set(varIntakeUncertain,!(Coalesce(varFlow.status,0) in [400,401,403,409,422])); Set(varMessage,If(Coalesce(varFlow.status,0)=409,"Intake
        blocked. Resume your unfinished intake, or ask an administrator to override a duplicate submitted within
        2 hours.","Submission outcome is unknown. Keep the text and compare this source ID in recent requests: "
        & varSourceId)); false,If(IsError(varFlow),If(IsError(varFlow),Set(varFlow,{ok:false,status:0,error_code:"TRANSPORT_UNKNOWN",error_message:"",request_id:"",result_json:""}));
        Set(varIntakeUncertain,!(Coalesce(varFlow.status,0) in [400,401,403,409,422])); Set(varMessage,If(Coalesce(varFlow.status,0)=409,"Intake
        blocked. Resume your unfinished intake, or ask an administrator to override a duplicate submitted within
        2 hours.","Submission outcome is unknown. Keep the text and compare this source ID in recent requests: "
        & varSourceId)); false,If(IsError(If(!varFlow.ok,Error({Kind:ErrorKind.Custom,Message:"Request failed"}))),If(IsError(varFlow),Set(varFlow,{ok:false,status:0,error_code:"TRANSPORT_UNKNOWN",error_message:"",request_id:"",result_json:""}));
        Set(varIntakeUncertain,!(Coalesce(varFlow.status,0) in [400,401,403,409,422])); Set(varMessage,If(Coalesce(varFlow.status,0)=409,"Intake
        blocked. Resume your unfinished intake, or ask an administrator to override a duplicate submitted within
        2 hours.","Submission outcome is unknown. Keep the text and compare this source ID in recent requests: "
        & varSourceId)); false,If(IsError(Set(varRaw,ParseJSON(varFlow.result_json))),If(IsError(varFlow),Set(varFlow,{ok:false,status:0,error_code:"TRANSPORT_UNKNOWN",error_message:"",request_id:"",result_json:""}));
        Set(varIntakeUncertain,!(Coalesce(varFlow.status,0) in [400,401,403,409,422])); Set(varMessage,If(Coalesce(varFlow.status,0)=409,"Intake
        blocked. Resume your unfinished intake, or ask an administrator to override a duplicate submitted within
        2 hours.","Submission outcome is unknown. Keep the text and compare this source ID in recent requests: "
        & varSourceId)); false,If(IsError(varRaw),If(IsError(varFlow),Set(varFlow,{ok:false,status:0,error_code:"TRANSPORT_UNKNOWN",error_message:"",request_id:"",result_json:""}));
        Set(varIntakeUncertain,!(Coalesce(varFlow.status,0) in [400,401,403,409,422])); Set(varMessage,If(Coalesce(varFlow.status,0)=409,"Intake
        blocked. Resume your unfinished intake, or ask an administrator to override a duplicate submitted within
        2 hours.","Submission outcome is unknown. Keep the text and compare this source ID in recent requests: "
        & varSourceId)); false,If(IsError(Set(varAck,{request_id:Text(varRaw.request_id),status:Text(varRaw.status),received_at:DateTimeValue(Text(varRaw.received_at)),records:Value(varRaw.records),rejected:Value(varRaw.rejected),requires_review:Value(varRaw.requires_review)})),If(IsError(varFlow),Set(varFlow,{ok:false,status:0,error_code:"TRANSPORT_UNKNOWN",error_message:"",request_id:"",result_json:""}));
        Set(varIntakeUncertain,!(Coalesce(varFlow.status,0) in [400,401,403,409,422])); Set(varMessage,If(Coalesce(varFlow.status,0)=409,"Intake
        blocked. Resume your unfinished intake, or ask an administrator to override a duplicate submitted within
        2 hours.","Submission outcome is unknown. Keep the text and compare this source ID in recent requests: "
        & varSourceId)); false,If(IsError(Set(varRequestId,varAck.request_id)),If(IsError(varFlow),Set(varFlow,{ok:false,status:0,error_code:"TRANSPORT_UNKNOWN",error_message:"",request_id:"",result_json:""}));
        Set(varIntakeUncertain,!(Coalesce(varFlow.status,0) in [400,401,403,409,422])); Set(varMessage,If(Coalesce(varFlow.status,0)=409,"Intake
        blocked. Resume your unfinished intake, or ask an administrator to override a duplicate submitted within
        2 hours.","Submission outcome is unknown. Keep the text and compare this source ID in recent requests: "
        & varSourceId)); false,If(IsError(Set(varActiveIntake,varAck.request_id)),If(IsError(varFlow),Set(varFlow,{ok:false,status:0,error_code:"TRANSPORT_UNKNOWN",error_message:"",request_id:"",result_json:""}));
        Set(varIntakeUncertain,!(Coalesce(varFlow.status,0) in [400,401,403,409,422])); Set(varMessage,If(Coalesce(varFlow.status,0)=409,"Intake
        blocked. Resume your unfinished intake, or ask an administrator to override a duplicate submitted within
        2 hours.","Submission outcome is unknown. Keep the text and compare this source ID in recent requests: "
        & varSourceId)); false,If(IsError(Set(varIntakeUncertain,false)),If(IsError(varFlow),Set(varFlow,{ok:false,status:0,error_code:"TRANSPORT_UNKNOWN",error_message:"",request_id:"",result_json:""}));
        Set(varIntakeUncertain,!(Coalesce(varFlow.status,0) in [400,401,403,409,422])); Set(varMessage,If(Coalesce(varFlow.status,0)=409,"Intake
        blocked. Resume your unfinished intake, or ask an administrator to override a duplicate submitted within
        2 hours.","Submission outcome is unknown. Keep the text and compare this source ID in recent requests: "
        & varSourceId)); false,If(IsError(Reset(chkDuplicateOverride)),If(IsError(varFlow),Set(varFlow,{ok:false,status:0,error_code:"TRANSPORT_UNKNOWN",error_message:"",request_id:"",result_json:""}));
        Set(varIntakeUncertain,!(Coalesce(varFlow.status,0) in [400,401,403,409,422])); Set(varMessage,If(Coalesce(varFlow.status,0)=409,"Intake
        blocked. Resume your unfinished intake, or ask an administrator to override a duplicate submitted within
        2 hours.","Submission outcome is unknown. Keep the text and compare this source ID in recent requests: "
        & varSourceId)); false,If(IsError(Reset(txtOverrideReason)),If(IsError(varFlow),Set(varFlow,{ok:false,status:0,error_code:"TRANSPORT_UNKNOWN",error_message:"",request_id:"",result_json:""}));
        Set(varIntakeUncertain,!(Coalesce(varFlow.status,0) in [400,401,403,409,422])); Set(varMessage,If(Coalesce(varFlow.status,0)=409,"Intake
        blocked. Resume your unfinished intake, or ask an administrator to override a duplicate submitted within
        2 hours.","Submission outcome is unknown. Keep the text and compare this source ID in recent requests: "
        & varSourceId)); false,If(IsError(Set(varMessage,"Intake saved. Load results to inspect validation.")),If(IsError(varFlow),Set(varFlow,{ok:false,status:0,error_code:"TRANSPORT_UNKNOWN",error_message:"",request_id:"",result_json:""}));
        Set(varIntakeUncertain,!(Coalesce(varFlow.status,0) in [400,401,403,409,422])); Set(varMessage,If(Coalesce(varFlow.status,0)=409,"Intake
        blocked. Resume your unfinished intake, or ask an administrator to override a duplicate submitted within
        2 hours.","Submission outcome is unknown. Keep the text and compare this source ID in recent requests: "
        & varSourceId)); false,If(IsError(Navigate(scrResults)),If(IsError(varFlow),Set(varFlow,{ok:false,status:0,error_code:"TRANSPORT_UNKNOWN",error_message:"",request_id:"",result_json:""}));
        Set(varIntakeUncertain,!(Coalesce(varFlow.status,0) in [400,401,403,409,422])); Set(varMessage,If(Coalesce(varFlow.status,0)=409,"Intake
        blocked. Resume your unfinished intake, or ask an administrator to override a duplicate submitted within
        2 hours.","Submission outcome is unknown. Keep the text and compare this source ID in recent requests: "
        & varSourceId)); false,true)))))))))))))); Set(varBusy,false))'
      DisplayMode: =If(Coalesce(varBusy,false) || !Coalesce(varEnvironmentReady,false) || Coalesce(varIntakeUncertain,false)
        || !IsBlank(varActiveIntake) || IsBlank(Trim(txtSource.Text)) || (chkDuplicateOverride.Value && IsBlank(Trim(txtOverrideReason.Text))),DisplayMode.Disabled,DisplayMode.Edit)
      TabIndex: =0
      FocusedBorderThickness: =3
      FocusedBorderColor: =Self.Color
- btnRecent:
    Control: Classic/Button
    Properties:
      X: =254
      Y: =490
      Width: =210
      Height: =44
      Text: ="Load recent requests"
      OnSelect: =If(!Coalesce(varBusy,false) && Coalesce(varEnvironmentReady,false),Set(varBusy,true); Set(varFlow,Blank());
        If(IsError(Set(varFlow,MILSTRIPStageBroker.Run("ListIntakeRequests",JSON({limit:50},JSONFormat.Compact)))),If(IsError(varFlow),Set(varFlow,{ok:false,status:0,error_code:"TRANSPORT_UNKNOWN",error_message:"",request_id:"",result_json:""}));
        Set(varMessage,"Requests unavailable. Input and pagination retained."); false,If(IsError(varFlow),If(IsError(varFlow),Set(varFlow,{ok:false,status:0,error_code:"TRANSPORT_UNKNOWN",error_message:"",request_id:"",result_json:""}));
        Set(varMessage,"Requests unavailable. Input and pagination retained."); false,If(IsError(If(!varFlow.ok,Error({Kind:ErrorKind.Custom,Message:"Request
        failed"}))),If(IsError(varFlow),Set(varFlow,{ok:false,status:0,error_code:"TRANSPORT_UNKNOWN",error_message:"",request_id:"",result_json:""}));
        Set(varMessage,"Requests unavailable. Input and pagination retained."); false,If(IsError(Set(varRaw,ParseJSON(varFlow.result_json))),If(IsError(varFlow),Set(varFlow,{ok:false,status:0,error_code:"TRANSPORT_UNKNOWN",error_message:"",request_id:"",result_json:""}));
        Set(varMessage,"Requests unavailable. Input and pagination retained."); false,If(IsError(varRaw),If(IsError(varFlow),Set(varFlow,{ok:false,status:0,error_code:"TRANSPORT_UNKNOWN",error_message:"",request_id:"",result_json:""}));
        Set(varMessage,"Requests unavailable. Input and pagination retained."); false,If(IsError(ClearCollect(colRequests,ForAll(Table(varRaw.items)
        As row,{request_id:Text(row.Value.request_id),source_type:Text(row.Value.source_type),source_id:Text(row.Value.source_id),status:Text(row.Value.status),received_at:DateTimeValue(Text(row.Value.received_at)),completed_at:If(IsBlank(Text(row.Value.completed_at)),Blank(),DateTimeValue(Text(row.Value.completed_at)))}))),If(IsError(varFlow),Set(varFlow,{ok:false,status:0,error_code:"TRANSPORT_UNKNOWN",error_message:"",request_id:"",result_json:""}));
        Set(varMessage,"Requests unavailable. Input and pagination retained."); false,If(IsError(Set(varRequestsCursor,Text(varRaw.next_cursor))),If(IsError(varFlow),Set(varFlow,{ok:false,status:0,error_code:"TRANSPORT_UNKNOWN",error_message:"",request_id:"",result_json:""}));
        Set(varMessage,"Requests unavailable. Input and pagination retained."); false,true))))))); Set(varBusy,false))
      DisplayMode: =If(!Coalesce(varEnvironmentReady,false),DisplayMode.Disabled,If(Coalesce(varBusy,false),DisplayMode.Disabled,DisplayMode.Edit))
      TabIndex: =0
      FocusedBorderThickness: =3
      FocusedBorderColor: =Self.Color
- btnIntakeRetryConfirm:
    Control: Classic/Button
    Properties:
      X: =484
      Y: =490
      Width: =210
      Height: =44
      Text: = "Resume / new intake"
      OnSelect: '=If(!Coalesce(varBusy,false) && Coalesce(varEnvironmentReady,false),Set(varBusy,true); Set(varFlow,Blank());
        If(IsError(Set(varFlow,MILSTRIPStageBroker.Run("GetIntakeWorkflow",JSON({source_id:If(Coalesce(varIntakeUncertain,false),varSourceId,Blank())},JSONFormat.Compact)))),If(IsError(varFlow),Set(varFlow,{ok:false,status:0,error_code:"TRANSPORT_UNKNOWN",error_message:"",request_id:"",result_json:""}));
        Set(varMessage,"Workflow status unavailable. Keep the current intake and retry."); false,If(IsError(varFlow),If(IsError(varFlow),Set(varFlow,{ok:false,status:0,error_code:"TRANSPORT_UNKNOWN",error_message:"",request_id:"",result_json:""}));
        Set(varMessage,"Workflow status unavailable. Keep the current intake and retry."); false,If(IsError(If(!varFlow.ok,Error({Kind:ErrorKind.Custom,Message:"Request
        failed"}))),If(IsError(varFlow),Set(varFlow,{ok:false,status:0,error_code:"TRANSPORT_UNKNOWN",error_message:"",request_id:"",result_json:""}));
        Set(varMessage,"Workflow status unavailable. Keep the current intake and retry."); false,If(IsError(Set(varWorkflowRaw,ParseJSON(varFlow.result_json))),If(IsError(varFlow),Set(varFlow,{ok:false,status:0,error_code:"TRANSPORT_UNKNOWN",error_message:"",request_id:"",result_json:""}));
        Set(varMessage,"Workflow status unavailable. Keep the current intake and retry."); false,If(IsError(varWorkflowRaw),If(IsError(varFlow),Set(varFlow,{ok:false,status:0,error_code:"TRANSPORT_UNKNOWN",error_message:"",request_id:"",result_json:""}));
        Set(varMessage,"Workflow status unavailable. Keep the current intake and retry."); false,If(IsError(Set(varActiveIntake,Text(varWorkflowRaw.active_request_id))),If(IsError(varFlow),Set(varFlow,{ok:false,status:0,error_code:"TRANSPORT_UNKNOWN",error_message:"",request_id:"",result_json:""}));
        Set(varMessage,"Workflow status unavailable. Keep the current intake and retry."); false,If(IsError(If(Coalesce(varIntakeUncertain,false),If(Text(varWorkflowRaw.source_resolution)="matched"
        && CountRows(Table(varWorkflowRaw.source_receipts))=1,Set(varRecoveredIntake,First(Table(varWorkflowRaw.source_receipts)).Value);
        If(Text(varRecoveredIntake.source_id)=varSourceId,Set(varAck,{request_id:Text(varRecoveredIntake.request_id),status:Text(varRecoveredIntake.status),received_at:DateTimeValue(Text(varRecoveredIntake.received_at)),records:Value(varRecoveredIntake.records),rejected:Value(varRecoveredIntake.rejected),requires_review:Value(varRecoveredIntake.requires_review)});
        Set(varRequestId,varAck.request_id); Set(varIntakeUncertain,false); Reset(chkDuplicateOverride); Reset(txtOverrideReason);
        Set(varMessage,"Saved intake confirmed by its source ID. Load results to inspect it."); Navigate(scrResults),Set(varMessage,"Receipt
        did not match this source ID. Keep the submission and contact an administrator.")),Set(varMessage,"Submission
        remains unconfirmed. Keep the text and source ID: " & varSourceId & ". Check again later or contact an administrator;
        the original request may still be running.")),If(!Boolean(varWorkflowRaw.can_start),Set(varRequestId,varActiveIntake);
        Navigate(scrResults),Reset(txtSource); Reset(chkDuplicateOverride); Reset(txtOverrideReason); Set(varSourceId,Blank());
        Set(varAck,Blank()); Set(varRequestId,Blank()); Set(varMessage,"Ready for a new intake. Identical content
        is blocked for 2 hours.")))),If(IsError(varFlow),Set(varFlow,{ok:false,status:0,error_code:"TRANSPORT_UNKNOWN",error_message:"",request_id:"",result_json:""}));
        Set(varMessage,"Workflow status unavailable. Keep the current intake and retry."); false,true))))))); Set(varBusy,false))'
      DisplayMode: =If(Coalesce(varBusy,false) || !Coalesce(varEnvironmentReady,false),DisplayMode.Disabled,DisplayMode.Edit)
      TabIndex: =0
      FocusedBorderThickness: =3
      FocusedBorderColor: =Self.Color
- galRequests:
    Control: Gallery
    Properties:
      X: =24
      Y: =548
      Width: =Parent.Width-278
      Height: =Parent.Height-564
      Items: =colRequests
      TemplateSize: =70
      AccessibleLabel: ="Recent intake requests"
      ItemAccessibleLabel: ="Request " & ThisItem.request_id & ", " & ThisItem.status & If(!IsBlank(ThisItem.source_id),",
        source " & ThisItem.source_id,"")
    Variant: Vertical
    Children:
    - RequestRow:
        Control: Classic/Button
        Properties:
          X: =0
          Y: =0
          Width: =Parent.TemplateWidth
          Height: =64
          Text: ="Open request " & Coalesce(ThisItem.source_id,ThisItem.request_id) & " | " & ThisItem.status
          OnSelect: =Set(varRequestId,ThisItem.request_id); Set(varMessage,""); Navigate(scrResults)
          DisplayMode: =If(!Coalesce(varEnvironmentReady,false),DisplayMode.Disabled,DisplayMode.Edit)
          TabIndex: =0
          FocusedBorderThickness: =3
          FocusedBorderColor: =Self.Color
- btnMoreRequests:
    Control: Classic/Button
    Properties:
      X: =Parent.Width-234
      Y: =548
      Width: =210
      Height: =44
      Text: ="More requests"
      OnSelect: =If(!Coalesce(varBusy,false) && Coalesce(varEnvironmentReady,false),Set(varBusy,true); Set(varFlow,Blank());
        If(IsError(Set(varFlow,MILSTRIPStageBroker.Run("ListIntakeRequests",JSON({limit:50,cursor:varRequestsCursor},JSONFormat.Compact)))),If(IsError(varFlow),Set(varFlow,{ok:false,status:0,error_code:"TRANSPORT_UNKNOWN",error_message:"",request_id:"",result_json:""}));
        Set(varMessage,"Requests unavailable. Input and pagination retained."); false,If(IsError(varFlow),If(IsError(varFlow),Set(varFlow,{ok:false,status:0,error_code:"TRANSPORT_UNKNOWN",error_message:"",request_id:"",result_json:""}));
        Set(varMessage,"Requests unavailable. Input and pagination retained."); false,If(IsError(If(!varFlow.ok,Error({Kind:ErrorKind.Custom,Message:"Request
        failed"}))),If(IsError(varFlow),Set(varFlow,{ok:false,status:0,error_code:"TRANSPORT_UNKNOWN",error_message:"",request_id:"",result_json:""}));
        Set(varMessage,"Requests unavailable. Input and pagination retained."); false,If(IsError(Set(varRaw,ParseJSON(varFlow.result_json))),If(IsError(varFlow),Set(varFlow,{ok:false,status:0,error_code:"TRANSPORT_UNKNOWN",error_message:"",request_id:"",result_json:""}));
        Set(varMessage,"Requests unavailable. Input and pagination retained."); false,If(IsError(varRaw),If(IsError(varFlow),Set(varFlow,{ok:false,status:0,error_code:"TRANSPORT_UNKNOWN",error_message:"",request_id:"",result_json:""}));
        Set(varMessage,"Requests unavailable. Input and pagination retained."); false,If(IsError(Collect(colRequests,ForAll(Table(varRaw.items)
        As row,{request_id:Text(row.Value.request_id),source_type:Text(row.Value.source_type),source_id:Text(row.Value.source_id),status:Text(row.Value.status),received_at:DateTimeValue(Text(row.Value.received_at)),completed_at:If(IsBlank(Text(row.Value.completed_at)),Blank(),DateTimeValue(Text(row.Value.completed_at)))}))),If(IsError(varFlow),Set(varFlow,{ok:false,status:0,error_code:"TRANSPORT_UNKNOWN",error_message:"",request_id:"",result_json:""}));
        Set(varMessage,"Requests unavailable. Input and pagination retained."); false,If(IsError(Set(varRequestsCursor,Text(varRaw.next_cursor))),If(IsError(varFlow),Set(varFlow,{ok:false,status:0,error_code:"TRANSPORT_UNKNOWN",error_message:"",request_id:"",result_json:""}));
        Set(varMessage,"Requests unavailable. Input and pagination retained."); false,true))))))); Set(varBusy,false))
      DisplayMode: =If(!Coalesce(varEnvironmentReady,false),DisplayMode.Disabled,If(Coalesce(varBusy,false) || IsBlank(varRequestsCursor),DisplayMode.Disabled,DisplayMode.Edit))
      TabIndex: =0
      FocusedBorderThickness: =3
      FocusedBorderColor: =Self.Color
- EnvironmentStatus:
    Control: Label
    Properties:
      X: =24
      Y: =110
      Width: =Parent.Width-282
      Height: =36
      Text: =Upper(AppEnvironment) & " | " & If(Coalesce(varEnvironmentReady,false),"Ready | " & varEnvironment.target_label,"Unavailable")
      Color: =If(Coalesce(varEnvironmentReady,false),Color.DarkGreen,Color.DarkRed)
      Size: =13
- btnCheckEnvironment:
    Control: Classic/Button
    Properties:
      X: =Parent.Width-234
      Y: =110
      Width: =210
      Height: =36
      Text: ="Check connection"
      OnSelect: =If(!Coalesce(varBusy,false),Set(varBusy,true); Set(varFlow,Blank()); If(IsError(Set(varEnvironmentReady,false)),If(IsError(varFlow),Set(varFlow,{ok:false,status:0,error_code:"TRANSPORT_UNKNOWN",error_message:"",request_id:"",result_json:""}));
        Set(varEnvironmentReady,false); Set(varMessage,"Connection unavailable. Configuration and Users remain available
        to authorized administrators."); false,If(IsError(Clear(colCapabilities)),If(IsError(varFlow),Set(varFlow,{ok:false,status:0,error_code:"TRANSPORT_UNKNOWN",error_message:"",request_id:"",result_json:""}));
        Set(varEnvironmentReady,false); Set(varMessage,"Connection unavailable. Configuration and Users remain available
        to authorized administrators."); false,If(IsError(Set(varFlow,MILSTRIPStageBroker.Run("GetCurrentUser",JSON({},JSONFormat.Compact)))),If(IsError(varFlow),Set(varFlow,{ok:false,status:0,error_code:"TRANSPORT_UNKNOWN",error_message:"",request_id:"",result_json:""}));
        Set(varEnvironmentReady,false); Set(varMessage,"Connection unavailable. Configuration and Users remain available
        to authorized administrators."); false,If(IsError(varFlow),If(IsError(varFlow),Set(varFlow,{ok:false,status:0,error_code:"TRANSPORT_UNKNOWN",error_message:"",request_id:"",result_json:""}));
        Set(varEnvironmentReady,false); Set(varMessage,"Connection unavailable. Configuration and Users remain available
        to authorized administrators."); false,If(IsError(If(!varFlow.ok,Error({Kind:ErrorKind.Custom,Message:"Request
        failed"}))),If(IsError(varFlow),Set(varFlow,{ok:false,status:0,error_code:"TRANSPORT_UNKNOWN",error_message:"",request_id:"",result_json:""}));
        Set(varEnvironmentReady,false); Set(varMessage,"Connection unavailable. Configuration and Users remain available
        to authorized administrators."); false,If(IsError(Set(varIdentityRaw,ParseJSON(varFlow.result_json))),If(IsError(varFlow),Set(varFlow,{ok:false,status:0,error_code:"TRANSPORT_UNKNOWN",error_message:"",request_id:"",result_json:""}));
        Set(varEnvironmentReady,false); Set(varMessage,"Connection unavailable. Configuration and Users remain available
        to authorized administrators."); false,If(IsError(varIdentityRaw),If(IsError(varFlow),Set(varFlow,{ok:false,status:0,error_code:"TRANSPORT_UNKNOWN",error_message:"",request_id:"",result_json:""}));
        Set(varEnvironmentReady,false); Set(varMessage,"Connection unavailable. Configuration and Users remain available
        to authorized administrators."); false,If(IsError(Set(varCurrentUser,{display_name:Text(varIdentityRaw.display_name),role:Text(varIdentityRaw.role),environment:Text(varIdentityRaw.environment)})),If(IsError(varFlow),Set(varFlow,{ok:false,status:0,error_code:"TRANSPORT_UNKNOWN",error_message:"",request_id:"",result_json:""}));
        Set(varEnvironmentReady,false); Set(varMessage,"Connection unavailable. Configuration and Users remain available
        to authorized administrators."); false,If(IsError(If(varCurrentUser.environment<>AppEnvironment,Error({Kind:ErrorKind.Custom,Message:"Wrong
        environment"}))),If(IsError(varFlow),Set(varFlow,{ok:false,status:0,error_code:"TRANSPORT_UNKNOWN",error_message:"",request_id:"",result_json:""}));
        Set(varEnvironmentReady,false); Set(varMessage,"Connection unavailable. Configuration and Users remain available
        to authorized administrators."); false,If(IsError(ClearCollect(colCapabilities,ForAll(Table(varIdentityRaw.capabilities),{Value:Text(Value)}))),If(IsError(varFlow),Set(varFlow,{ok:false,status:0,error_code:"TRANSPORT_UNKNOWN",error_message:"",request_id:"",result_json:""}));
        Set(varEnvironmentReady,false); Set(varMessage,"Connection unavailable. Configuration and Users remain available
        to authorized administrators."); false,If(IsError(Set(varFlow,MILSTRIPStageBroker.Run("GetHealth",JSON({},JSONFormat.Compact)))),If(IsError(varFlow),Set(varFlow,{ok:false,status:0,error_code:"TRANSPORT_UNKNOWN",error_message:"",request_id:"",result_json:""}));
        Set(varEnvironmentReady,false); Set(varMessage,"Connection unavailable. Configuration and Users remain available
        to authorized administrators."); false,If(IsError(varFlow),If(IsError(varFlow),Set(varFlow,{ok:false,status:0,error_code:"TRANSPORT_UNKNOWN",error_message:"",request_id:"",result_json:""}));
        Set(varEnvironmentReady,false); Set(varMessage,"Connection unavailable. Configuration and Users remain available
        to authorized administrators."); false,If(IsError(If(!varFlow.ok,Error({Kind:ErrorKind.Custom,Message:"Request
        failed"}))),If(IsError(varFlow),Set(varFlow,{ok:false,status:0,error_code:"TRANSPORT_UNKNOWN",error_message:"",request_id:"",result_json:""}));
        Set(varEnvironmentReady,false); Set(varMessage,"Connection unavailable. Configuration and Users remain available
        to authorized administrators."); false,If(IsError(Set(varHealthRaw,ParseJSON(varFlow.result_json))),If(IsError(varFlow),Set(varFlow,{ok:false,status:0,error_code:"TRANSPORT_UNKNOWN",error_message:"",request_id:"",result_json:""}));
        Set(varEnvironmentReady,false); Set(varMessage,"Connection unavailable. Configuration and Users remain available
        to authorized administrators."); false,If(IsError(varHealthRaw),If(IsError(varFlow),Set(varFlow,{ok:false,status:0,error_code:"TRANSPORT_UNKNOWN",error_message:"",request_id:"",result_json:""}));
        Set(varEnvironmentReady,false); Set(varMessage,"Connection unavailable. Configuration and Users remain available
        to authorized administrators."); false,If(IsError(Set(varEnvironment,{environment:Text(varHealthRaw.environment),target_label:Text(varHealthRaw.target_label),ready:Boolean(varHealthRaw.ready)})),If(IsError(varFlow),Set(varFlow,{ok:false,status:0,error_code:"TRANSPORT_UNKNOWN",error_message:"",request_id:"",result_json:""}));
        Set(varEnvironmentReady,false); Set(varMessage,"Connection unavailable. Configuration and Users remain available
        to authorized administrators."); false,If(IsError(Set(varEnvironmentReady,varEnvironment.environment=AppEnvironment
        && varEnvironment.ready)),If(IsError(varFlow),Set(varFlow,{ok:false,status:0,error_code:"TRANSPORT_UNKNOWN",error_message:"",request_id:"",result_json:""}));
        Set(varEnvironmentReady,false); Set(varMessage,"Connection unavailable. Configuration and Users remain available
        to authorized administrators."); false,If(IsError(Set(varFlow,MILSTRIPStageBroker.Run("GetIntakeWorkflow",JSON({},JSONFormat.Compact)))),If(IsError(varFlow),Set(varFlow,{ok:false,status:0,error_code:"TRANSPORT_UNKNOWN",error_message:"",request_id:"",result_json:""}));
        Set(varEnvironmentReady,false); Set(varMessage,"Connection unavailable. Configuration and Users remain available
        to authorized administrators."); false,If(IsError(varFlow),If(IsError(varFlow),Set(varFlow,{ok:false,status:0,error_code:"TRANSPORT_UNKNOWN",error_message:"",request_id:"",result_json:""}));
        Set(varEnvironmentReady,false); Set(varMessage,"Connection unavailable. Configuration and Users remain available
        to authorized administrators."); false,If(IsError(If(!varFlow.ok,Error({Kind:ErrorKind.Custom,Message:"Request
        failed"}))),If(IsError(varFlow),Set(varFlow,{ok:false,status:0,error_code:"TRANSPORT_UNKNOWN",error_message:"",request_id:"",result_json:""}));
        Set(varEnvironmentReady,false); Set(varMessage,"Connection unavailable. Configuration and Users remain available
        to authorized administrators."); false,If(IsError(Set(varWorkflowRaw,ParseJSON(varFlow.result_json))),If(IsError(varFlow),Set(varFlow,{ok:false,status:0,error_code:"TRANSPORT_UNKNOWN",error_message:"",request_id:"",result_json:""}));
        Set(varEnvironmentReady,false); Set(varMessage,"Connection unavailable. Configuration and Users remain available
        to authorized administrators."); false,If(IsError(varWorkflowRaw),If(IsError(varFlow),Set(varFlow,{ok:false,status:0,error_code:"TRANSPORT_UNKNOWN",error_message:"",request_id:"",result_json:""}));
        Set(varEnvironmentReady,false); Set(varMessage,"Connection unavailable. Configuration and Users remain available
        to authorized administrators."); false,If(IsError(Set(varActiveIntake,Text(varWorkflowRaw.active_request_id))),If(IsError(varFlow),Set(varFlow,{ok:false,status:0,error_code:"TRANSPORT_UNKNOWN",error_message:"",request_id:"",result_json:""}));
        Set(varEnvironmentReady,false); Set(varMessage,"Connection unavailable. Configuration and Users remain available
        to authorized administrators."); false,If(IsError(Set(varMessage,"Connection checked.")),If(IsError(varFlow),Set(varFlow,{ok:false,status:0,error_code:"TRANSPORT_UNKNOWN",error_message:"",request_id:"",result_json:""}));
        Set(varEnvironmentReady,false); Set(varMessage,"Connection unavailable. Configuration and Users remain available
        to authorized administrators."); false,true)))))))))))))))))))))))); Set(varBusy,false))
      DisplayMode: =If(Coalesce(varBusy,false),DisplayMode.Disabled,DisplayMode.Edit)
      TabIndex: =0
      FocusedBorderThickness: =3
      FocusedBorderColor: =Self.Color
- btnConfiguration:
    Control: Classic/Button
    Properties:
      X: =Parent.Width-464
      Y: =22
      Width: =210
      Height: =44
      Text: ="Configuration"
      OnSelect: =Navigate(scrConfiguration)
      DisplayMode: =If(Coalesce(varBusy,false),DisplayMode.Disabled,DisplayMode.Edit)
      Visible: ="runtime.configure" in colCapabilities.Value
      TabIndex: =0
      FocusedBorderThickness: =3
      FocusedBorderColor: =Self.Color
- btnUsers:
    Control: Classic/Button
    Properties:
      X: =Parent.Width-234
      Y: =22
      Width: =210
      Height: =44
      Text: ="Users"
      OnSelect: =Navigate(scrUsers)
      DisplayMode: =If(Coalesce(varBusy,false),DisplayMode.Disabled,DisplayMode.Edit)
      Visible: ="users.manage" in colCapabilities.Value
      TabIndex: =0
      FocusedBorderThickness: =3
      FocusedBorderColor: =Self.Color
- chkDuplicateOverride:
    Control: Classic/CheckBox
    Properties:
      X: =24
      Y: =416
      Width: =290
      Height: =44
      Text: ="Override 2-hour duplicate block"
      Default: =false
      Visible: ="runtime.configure" in colCapabilities.Value
      DisplayMode: =If(Coalesce(varBusy,false) || Coalesce(varIntakeUncertain,false),DisplayMode.Disabled,DisplayMode.Edit)
      TabIndex: =0
      FocusedBorderThickness: =3
      FocusedBorderColor: =RGBA(0,0,0,1)
- txtOverrideReason:
    Control: Classic/TextInput
    Properties:
      X: =330
      Y: =416
      Width: =Parent.Width-354
      Height: =44
      Default: =""
      HintText: ="Required override reason (recorded in audit)"
      MaxLength: =1000
      Visible: ="runtime.configure" in colCapabilities.Value && chkDuplicateOverride.Value
      DisplayMode: =If(Coalesce(varBusy,false) || Coalesce(varIntakeUncertain,false),DisplayMode.Disabled,DisplayMode.Edit)
      AccessibleLabel: ="Audited duplicate override reason"
      TabIndex: =0
      FocusedBorderThickness: =3
      FocusedBorderColor: =RGBA(0,0,0,1)
