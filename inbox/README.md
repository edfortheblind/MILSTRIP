# Requirements inbox

Place the centralized change document and supporting files here when ready.
New documents in this folder are local and ignored by Git, except this README.
The existing reference files already tracked by Git remain versioned.

Incoming documents are proposed changes. Review them against the current app,
database configuration and SOP before scheduling implementation or publication.
Do not place credentials here; use the private `.cred` folder.
